# Copy file này thành .env rồi điền giá trị thật.
# .env KHÔNG được commit (đã có trong .gitignore); .env.example thì có.

DB_HOST=localhost
DB_PORT=5932
DB_NAME=week1_rbac
DB_USER=week1_app
DB_PASSWORD=week1_secret

# Tuỳ chọn: đặt nguyên chuỗi kết nối, sẽ được ưu tiên hơn 5 biến DB_* ở trên.
# ConnectionStrings__Default=Host=localhost;Port=5932;Database=week1_rbac;Username=week1_app;Password=week1_secret
